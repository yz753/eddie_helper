[project]
name = "eddie-helper"
version = "0.1.0"
description = "Utility package for using the EDDIE supercomputer system at the University of Edinburgh"
readme = "README.md"
authors = [
    { name = "chrishalcrow", email = "chrishalcrow@gmail.com" }
]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
eddie-helper = "eddie_helper:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
